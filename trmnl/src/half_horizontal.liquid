<div class="view view--half-horizontal">
  <div class="layout layout--column">

    {% if game %}

      <!-- Header -->
      <div class="header">
        <h2>🏀 BEST GAME</h2>
      </div>

      <!-- Content -->
      <div class="content flex-grow">

        <!-- Date -->
        <div class="game-date">{{ game.game_date }}</div>

        <!-- Score -->
        <div class="game-score">
          <div class="team">
            <span class="team__abbr">{{ game.away_team.abbr }}</span>
          </div>
          <span class="vs">@</span>
          <div class="team">
            <span class="team__abbr">{{ game.home_team.abbr }}</span>
          </div>
        </div>

      </div>

      <!-- Footer -->
      <div class="footer">
        <p>Updated: {{ updated_at | default: "recently" }}</p>
      </div>

    {% else %}

      <!-- Empty State -->
      <div class="empty">
        <div class="empty__icon">🏀</div>
        <div class="empty__message">No games</div>
      </div>

    {% endif %}

  </div>
</div>

<style>
  .view--half-horizontal {
    padding: 15px;
    height: 100%;
    display: flex;
    flex-direction: column;
  }

  .header h2 {
    font-size: 1.3rem;
    font-weight: bold;
    margin: 0 0 1rem 0;
    padding-bottom: 0.5rem;
    border-bottom: 2px solid #000;
    text-align: center;
  }

  .flex-grow {
    flex-grow: 1;
  }

  .game-date {
    text-align: center;
    font-size: 0.85rem;
    opacity: 0.7;
    margin-bottom: 1rem;
  }

  .game-score {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 1.5rem;
    margin: 1.5rem 0;
  }

  .team {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
  }

  .team__abbr {
    font-size: 1.1rem;
    font-weight: 600;
  }

  .team__score {
    font-size: 2rem;
    font-weight: bold;
  }

  .vs {
    font-size: 1.2rem;
    opacity: 0.5;
  }

  .empty {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    height: 100%;
  }

  .empty__icon {
    font-size: 3rem;
    margin-bottom: 0.5rem;
  }

  .empty__message {
    font-size: 1.2rem;
    font-weight: 600;
    opacity: 0.6;
  }

  .footer {
    margin-top: auto;
    padding-top: 0.8rem;
    border-top: 1px solid #ddd;
    text-align: center;
    font-size: 0.75rem;
    opacity: 0.6;
  }

  .footer p {
    margin: 0;
  }
</style>
