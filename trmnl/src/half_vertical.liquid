<div class="view view--half-vertical">
  <div class="layout layout--column">

    {% if game %}

      <!-- Header -->
      <div class="header">
        <h2>🏀</h2>
        <div class="subtitle">BEST GAME</div>
      </div>

      <!-- Content -->
      <div class="content flex-grow">

        <!-- Teams -->
        <div class="teams">
          <div class="team-item">
            <div class="team-abbr">{{ game.away_team.abbr }}</div>
          </div>
          <div class="separator">@</div>
          <div class="team-item">
            <div class="team-abbr">{{ game.home_team.abbr }}</div>
          </div>
        </div>

        <!-- Date -->
        <div class="date">{{ game.game_date }}</div>

      </div>

      <!-- Footer -->
      <div class="footer">
        <p>Updated: {{ updated_at | default: "recently" }}</p>
      </div>

    {% else %}

      <!-- Empty State -->
      <div class="empty">
        <div class="empty__icon">🏀</div>
        <div class="empty__text">No Games</div>
      </div>

    {% endif %}

  </div>
</div>

<style>
  .view--half-vertical {
    padding: 12px;
    height: 100%;
    display: flex;
    flex-direction: column;
  }

  .header {
    text-align: center;
    margin-bottom: 1rem;
    padding-bottom: 0.8rem;
    border-bottom: 2px solid #000;
  }

  .flex-grow {
    flex-grow: 1;
  }

  .header h2 {
    font-size: 2rem;
    margin: 0;
  }

  .subtitle {
    font-size: 0.9rem;
    font-weight: 600;
    margin-top: 0.3rem;
    letter-spacing: 0.05em;
  }

  .teams {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    margin: 1rem 0;
  }

  .team-item {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0.8rem;
    background: #f5f5f5;
    border-radius: 4px;
  }

  .team-abbr {
    font-size: 1.1rem;
    font-weight: 600;
  }

  .team-score {
    font-size: 1.8rem;
    font-weight: bold;
  }

  .separator {
    text-align: center;
    opacity: 0.5;
    font-size: 0.9rem;
    margin: 0.3rem 0;
  }

  .date {
    text-align: center;
    font-size: 0.8rem;
    opacity: 0.7;
    margin: 1rem 0;
  }

  .empty {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    height: 100%;
  }

  .empty__icon {
    font-size: 3rem;
    margin-bottom: 0.5rem;
  }

  .empty__text {
    font-size: 1rem;
    font-weight: 600;
    opacity: 0.6;
  }

  .footer {
    margin-top: auto;
    padding-top: 0.8rem;
    border-top: 1px solid #ddd;
    text-align: center;
    font-size: 0.7rem;
    opacity: 0.6;
  }

  .footer p {
    margin: 0;
  }
</style>
